DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"
REDIS_URL="redis://HOST:PORT"

# Web URL (for password reset links)
WEB_URL="https://yourdomain.com"

# Resend (transactional email — get key at resend.com)
RESEND_API_KEY="re_your_key_here"

# CORS (comma-separated, no spaces — defaults to localhost in dev)
CORS_ORIGINS="https://app.yourdomain.com,https://yourdomain.com"

# Auth (plain JWT — no Supabase)
JWT_SECRET="change-me-min-32-chars-random"

# AWS S3 (media uploads)
AWS_REGION="us-east-1"
AWS_S3_BUCKET=""
AWS_ACCESS_KEY_ID=""
AWS_SECRET_ACCESS_KEY=""

# Stripe (identity verification)
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""

# Google Places/Geocoding (SERVER-SIDE ONLY — proxied via /places, never shipped to clients)
# Restrict this key in Google Cloud to the Places API + Geocoding API, with quotas + a budget alert.
GOOGLE_PLACES_KEY=""

# API URL (consumed by mobile + web)
NEXT_PUBLIC_API_URL="https://api.yourdomain.com"
EXPO_PUBLIC_API_URL="https://api.yourdomain.com"

# Datadog APM
DD_SERVICE="salonin-api"
DD_ENV="production"
DD_AGENT_HOST="localhost"

# Sentry
SENTRY_DSN=""
SENTRY_ORG=""
SENTRY_PROJECT=""
SENTRY_AUTH_TOKEN=""
NEXT_PUBLIC_SENTRY_DSN=""
EXPO_PUBLIC_SENTRY_DSN=""
